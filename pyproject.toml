[project]
name = "utac-core"
version = "1.0.0"
description = "The mathematical UTAC logistic core - sigma(beta(R-Theta)), beta-Fitting, Frame-Principle and v_RIG for the GenesisAeon stack."
readme = "README.md"
license = "GPL-3.0-or-later"
authors = [{ name = "GenesisAeon Team", email = "team@genesisaeon.org" }]
requires-python = ">=3.11"
keywords = ["utac", "logistic", "field-theory", "frame-principle", "mathematics", "physics"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "sympy>=1.13.0",
    "numpy>=2.0.0",
    "rich>=13.0.0",
    "typer>=0.15.0",
]

[project.optional-dependencies]
stack = [
    "field-theory>=0.1.0",
    "sigillin>=1.0.0",
    "cosmic-moment>=1.0.0",
    "medium-modulation>=1.0.0",
    "entropy-governance>=1.0.0",
    "entropy-table>=2.0.0",
    "implosive-genesis>=1.0.0",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.9",
    "pre-commit>=3.7.0",
]
docs = [
    "mkdocs>=1.6",
    "mkdocstrings[python]>=0.27",
    "mkdocs-material>=9.6",
]

[project.scripts]
utac = "utac_core.cli:app"

[project.urls]
Repository = "https://github.com/GenesisAeon/utac-core"
Issues = "https://github.com/GenesisAeon/utac-core/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/utac_core"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "W", "UP", "C4", "SIM"]
ignore = ["B008"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=utac_core --cov-report=term-missing -v"

[tool.coverage.run]
source = ["src"]
omit = ["tests/*"]

